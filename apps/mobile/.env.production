# Chargé automatiquement pour les builds de production (NODE_ENV=production :
# `expo export`, builds natifs release, `eas build --profile production`).
EXPO_PUBLIC_WEBAPP_URL=https://app.tripbrain.fr
